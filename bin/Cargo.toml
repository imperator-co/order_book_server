[package]
name = "orderbook-server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
server = { path = "../server" }
env_logger = "0.11.8"
tokio = { version = "1", features = ["full"] }
clap = { version = "4.5.42", features = ["derive"] }
axum = "0.8.4"
log = "0.4"

[lints]
workspace = true
